export type Event = {
  id: string;
  name: string;
  description: string;
  time: string;
  type: string;
};

export type DayInWeek = {
  id: number;
  name: string;
};

export type PairTime = {
  id: number;
  time: string;
};