export enum AppRoute {
  HOME = "/",
  POSTS = "/posts",
  SIGNUP = "/signup",
  SCHEDULE = "/schedule",
  USERS = "/users",
  USER = "/user/:id",
  LOGIN = "/login",
  UPDATEUSERAVATAR = "/update-avatar",
  COURSES = "/courses",
}