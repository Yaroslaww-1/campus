export enum AppRoute {
  HOME = "/",
  POSTS = "/posts",
  LOGIN = "/login",
  SIGNUP = "/signup",
  SCHEDULE = "/schedule",
  USERS = "/users",
  USER = "/user/:id",
  UPDATEUSERAVATAR = "/update-avatar",
}